//Define and export a function called Catalog 
    //it should accept the harvested food array as input
    //it should iterate the array of foodObjects to be sold
    //As you iterate the array you are creating html for your website
        //the <main> element should have the inner html appended with an html rep of each food item
        //use ${} to show EACH food item through the array

import { harvestPlants } from "./harvester";

export const Catalog = (plantFoodReadytoSell) => {
    for (seeds of seedsHarvested) {
        
    }
}